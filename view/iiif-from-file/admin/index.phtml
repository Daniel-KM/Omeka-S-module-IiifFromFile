<?php
/**
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \IiifFromFile\Form\IiifFromFileForm $form
 */

$plugins = $this->getHelperPluginManager();
$translate = $plugins->get('translate');

$form->prepare();

$this->htmlElement('body')->appendAttribute('class', 'iiif-from-file');

$this->headScript()->appendFile($this->assetUrl('js/iiif-from-file.js', 'IiifFromFile'));
?>

<?= $this->pageTitle($translate('IIIF from File')) ?>

<p>
    <?= $translate('Export local items and media files to a remote repository.') ?>
</p>

<?= $this->form()->openTag($form) ?>

<div id="page-actions">
    <?= $this->formElement($form->get('submit')) ?>
</div>

<?php

foreach ($form as $element):
    $name = $element->getName();
    if ($name === 'submit') continue;
    echo $this->formRow($element);
endforeach;
?>

<?= $this->form()->closeTag() ?>
